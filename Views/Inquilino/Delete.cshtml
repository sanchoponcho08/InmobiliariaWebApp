@model InmobiliariaWebApp.Models.Inquilino

@{
    ViewData["Title"] = "Eliminar Inquilino";
}

<div class="row">
    <div class="col-md-8 offset-md-2">
        <div class="card border-danger">
            <div class="card-header bg-danger text-white">
                <h3 class="card-title">¿Estás seguro de que quieres eliminar este inquilino?</h3>
            </div>
            <div class="card-body">
                <p>Esta acción es irreversible. Por favor, confirma los detalles del inquilino a continuación antes de proceder.</p>
                <hr/>
                <dl class="row">
                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Nombre)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Nombre)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Apellido)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Apellido)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Dni)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Dni)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Telefono)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Telefono)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Email)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Email)</dd>
                </dl>
            </div>
            <div class="card-footer">
                 <form asp-action="Delete">
                    <input type="hidden" asp-for="Id" />
                    <input type="submit" value="Eliminar" class="btn btn-delete" />
                    <a asp-action="Index" class="btn btn-outline-secondary">Cancelar y Volver al Listado</a>
                </form>
            </div>
        </div>
    </div>
</div>
