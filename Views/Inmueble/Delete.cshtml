@model InmobiliariaWebApp.Models.Inmueble

@{
    ViewData["Title"] = "Eliminar Inmueble";
}

<div class="row">
    <div class="col-md-8 offset-md-2">
        <div class="card border-danger">
            <div class="card-header bg-danger text-white">
                <h3 class="card-title">¿Estás seguro de que quieres eliminar este inmueble?</h3>
            </div>
            <div class="card-body">
                <p>Esta acción es irreversible. Por favor, confirma los detalles del inmueble a continuación antes de proceder.</p>
                <hr/>
                <dl class="row">
                    <dt class="col-sm-3">Dueño</dt>
                    <dd class="col-sm-9">@(Model.Dueño?.Nombre) @(Model.Dueño?.Apellido)</dd>

                    <dt class="col-sm-3">Tipo</dt>
                    <dd class="col-sm-9">@(Model.Tipo?.Nombre)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Direccion)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Direccion)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Uso)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Uso)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Ambientes)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Ambientes)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Precio)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Precio)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Disponible)</dt>
                    <dd class="col-sm-9">@(Model.Disponible ? "Sí" : "No")</dd>
                </dl>
            </div>
            <div class="card-footer">
                 <form asp-action="Delete">
                    <input type="hidden" asp-for="Id" />
                    <input type="submit" value="Eliminar" class="btn btn-delete" />
                    <a asp-action="Index" class="btn btn-outline-secondary">Cancelar y Volver al Listado</a>
                </form>
            </div>
        </div>
    </div>
</div>
